Cayumi
abc123

test2
abc

https://getbootstrap.com/docs/3.3/getting-started/#examples

(venv) $ python
>>> app
Traceback (most recent call last):
  File "<stdin>", line 1, in <module>
NameError: name 'app' is not defined
>>>

The purpose of this command is to start a Python interpreter in the context of the application.
(venv) $ flask shell
>>> app
<Flask 'app'>


Libraries not in "batteries included"
flask-migration
email-validator


Enable debugger
set FLASK_DEBUG=1


Run fake smtp server in own terminal
python -m smtpd -n -c DebuggingServer localhost:8025
set export MAIL_SERVER=localhost
set MAIL_PORT=8025
This will show the generated email respone in terminal

use this to send for real:
set MAIL_SERVER=smtp.googlemail.com
set MAIL_PORT=587
set MAIL_USE_TLS=1
set MAIL_USERNAME=rallyengstrom@gmail.com
set MAIL_PASSWORD=ksr48ugpfesjnk1

errors
DEBUG, INFO, WARNING, ERROR and CRITICAL in increasing order of severity.



>>> users = User.query.all()
>>> for u in users:
...     db.session.delete(u)



h = Hero(name="Sprout", atk=34)
u[0].heroes.append(h)
db.session.commit()


foo = test.insert().values(hero_id=2, user_id=1, level=5)
db.session.execute(foo)
db.session.commit()

Hero commit: *ID*, 0, 0, 0, 0, USER ID, BASE ID

LIGHT KNIGHT!!

Query all ID's from Base
u = User.query.get(1)
base = Base.query.all()

for i in base:
    hero = Hero(level=0, awaken=0, wpn=0, medals=0, player=u, baseStats=i)
    db.session.add(hero)

    hero = Hero(level=0, awaken=0, wpn=0, medals=0, user_id=1, base_id=1)


      {% set count = namespace(value=0) %}
      {% for i in heroes %}
        {{ count.value }}
      {% set count.value = count.value + i.medals %}
      {% endfor %}

style="background:url('{{ url_for('static', filename='background-pattern.png') }}')"